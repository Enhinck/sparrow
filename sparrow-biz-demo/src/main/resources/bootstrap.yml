server:
    port: 8081 #启动端口
spring:
  profiles:
    active: dev
  #应用名称
  application:
    name: sparrow-biz-demo
# spring admin 访问无需验证
management:
   security:
      enabled: false

# 阿波罗配置
apollo:
  bootstrap:
    enabled: true
    namespaces: application,application.yml
app:
  id: sparrow

security:
  basic:
    enabled: false